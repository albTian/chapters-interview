export type ChapterJob = {
  position: string;
  job_url: string;
  id: number;
};

export type ChapterCandidate = {
  id: string;
  name: string;
  profile_image: string;
  tag: string;
  fit_score: number;
}