export type ChapterJob = {
  position: string;
  job_url: string;
  id: number;
};

export type ChapterCandidate = {
  name: string;
  profile_image: string;
  tag: string;
  fit_score: number;
  candidate_id: number;
}
